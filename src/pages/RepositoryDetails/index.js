import PropTypes from 'prop-types'
import React from 'react'
import { View, Text } from 'react-native'

const RepositoryDetails = ({ navigation }) => {
  return (
    <View>
      <Text>RepositoryDetails Screen</Text>
    </View>
  )
}

export default RepositoryDetails
